// Note: Sentinel data connectors are typically configured through the Azure portal
// or Azure REST API after Sentinel is enabled. This module serves as a placeholder
// for future connector configurations.

// Output placeholder for successful module deployment
output connectorsConfigured string = 'Sentinel data connectors should be configured manually through the Azure portal after deployment'
